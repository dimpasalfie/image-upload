import React from 'react';
import {ScrollView, View} from 'react-native';

const PendingImages = () => {
  return (
    <ScrollView contentInsetAdjustmentBehavior="automatic">
      <View>Pending Images</View>
    </ScrollView>
  );
};

export default PendingImages;
